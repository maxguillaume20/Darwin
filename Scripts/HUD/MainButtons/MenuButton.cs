using UnityEngine;
using System.Collections;
using UnityEngine.UI;
using RTS;

public class MenuButton : MainButton {
	  

	protected override void Start ()
	{
//		UAHPanel.SetMenuButton (this);
//		gameObject.GetComponent<Button>().onClick.AddListener( delegate {GameManager.Hud.OpenPauseMenu();});
	}
}
